{
  "name": "food-waste-management-mern",
  "private": true,
  "version": "1.0.0",
  "description": "MERN monorepo for Food Waste Management System (web + backend)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:web": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:all": "concurrently \"npm run dev:server\" \"npm run dev:web\"",
    "build:web": "npm run build --prefix client",
    "test:server": "npm run test --prefix server",
    "test": "npm run test:server"
  },
  "workspaces": [
    "server",
    "client",
    "packages/shared"
  ],
  "overrides": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "dependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
